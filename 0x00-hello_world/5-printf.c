#include <stdio.h>
/**
 * main - using prinf function to print a line
 * Return: returns 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
