#include "main.h"

/**
* _islower - Validates if a character is in lowercase
*
* @c: The character to be tested
*
* Return: 1 if the character is lowercase, 0 if not
*/

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
