#include "main.h"

/**
* _isdigit - Validates if a character is a numeral
*
* @c: The character to be examined
*
* Return: 1 if it is a number, otherwise 0
*/

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
